# Copy this file to .env.local for local development.
# Keep secrets out of git and never expose server-only values with NEXT_PUBLIC_.

# Server-only. Never commit a real key.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
